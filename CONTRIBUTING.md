# 增加与维护 skills

## 独立单元

每项能力使用 `skills/<lowercase-hyphen-id>/SKILL.md`。保持扁平目录，分类写在 `catalog.json`，避免一个 skill 的相对路径依赖另一项 skill 的内部文件。需要共享知识时先验证确有重复；优先链接权威文档，不引入没有调用方的公共框架。

入口必须包含 YAML `name` 和 `description`。名称与目录一致；描述明确何时使用，避免“任何任务都适用”。版本放在 `metadata.version`。复杂内容放入相对链接的 references，按任务需要读取；单一文件足够时不要创建空的 references/scripts/assets。

`agents/openai.yaml` 的 `default_prompt` 必须包含 `$skill-id`。保持默认可自动发现；仅在用户要求时限制隐式调用。不得把每次普通编辑都变成审批流程。

## 实战转规则

1. 记录可观察的失败，不只记录用户不满意。
2. 分开事实、根因推断、已经验证的修复和待验证的建议。
3. 提炼能改变实施决策的约束；保留例外与适用范围。
4. 把个人项目参数标记为案例参数，不能冒充通用行业标准。
5. 有必要才增加脚本；脚本要能拒绝坏输入，错误不能输出“通过”。
6. 用正例和故障注入验证检查器，再用真实任务做行为验收。自动检查不能代替审美、人体和设备判断。

更新 skill 时同步 metadata 版本、catalog（如分类变化）、入口路由和测试。整个集合的发布版本由插件清单管理，独立于单个 skill 版本。

## 公开边界

不提交会话原文、私有项目源码、美术原件、存档、访问令牌、内部域名、本机绝对路径或生成日志。案例写成原创复盘，引用第三方知识用出处链接，复用第三方文件需要另外审核其许可和声明。

推送前执行 README 中的本地检查，复查 `git diff --cached` 和文件清单。仓库检查器只捕获常见泄漏模式，人工审查仍需覆盖上下文。
